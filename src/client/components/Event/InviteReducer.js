
import { FETCH_INVITES_REQUEST, FETCH_INVITE_SUCCESS, FETCH_INVITE_FAILURE } from './InviteActions';

const initialState = {

};

const InviteReducer = (state = initialState, action) => {
  switch(action.type) {
    case FETCH_INVITES_REQUEST:
      break;
    
    case FETCH_INVITES_REQUEST:
      break;

    case FETCH_INVITES_REQUEST:
      break;

    default:
      return state;
  }
}



export default InviteReducer;
